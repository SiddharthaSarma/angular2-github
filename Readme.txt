Installation
---------------
1. Run the following command in command prompt to install angular-cli

npm install -g angular-cli

2. Clone the application and cd into project directory then do

npm install

Development server
---------------------------
To fireup the server run the following command

ng serve


